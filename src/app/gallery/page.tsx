import {getGallery} from '@/lib/data'; import Image from 'next/image';
export default async function Gallery(){const items=await getGallery();return <main className="section"><div className="wrap"><div className="head"><div><span className="kicker">WORKSHOP</span><h1>معرض الأعمال</h1></div><a className="gold" href="https://instagram.com/aslan_modelling" target="_blank" rel="noopener noreferrer">Instagram ↗</a></div>{items.length?<div className="grid three">{items.map(i=><article className="card" key={i.id}><div className="product-image"><Image src={i.image_url} alt={i.caption||'ASLAN MODELLING'} width={800} height={800}/></div><p>{i.caption}</p></article>)}</div>:<div className="card"><h2>المعرض قيد التجهيز</h2><p className="muted">ستظهر هنا أعمال الورشة التي ترفعها الإدارة.</p></div>}</div></main>}
