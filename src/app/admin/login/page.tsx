'use client';
import {useState} from 'react'; import {createClient} from '@/lib/supabase/client'; import {useRouter} from 'next/navigation';
export default function AdminLogin(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError('');const db=createClient();const {error}=await db.auth.signInWithPassword({email,password});if(error){setError('بيانات الدخول غير صحيحة.');return}router.replace('/admin/dashboard');router.refresh()}return <main className="section" style={{minHeight:'75vh',display:'grid',placeItems:'center'}}><form className="card" onSubmit={submit} style={{width:'min(430px,100%)',display:'grid',gap:14}}><span className="kicker">ASLAN ADMIN</span><h1>دخول الإدارة</h1><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="البريد الإلكتروني"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="كلمة السر"/><button className="btn primary" type="submit">دخول</button>{error&&<p style={{color:'#ef8b8b'}}>{error}</p>}</form></main>}
