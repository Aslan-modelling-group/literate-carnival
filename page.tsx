import Link from 'next/link'; import {site,whatsappLink} from '@/lib/config'; import {getProducts} from '@/lib/data';
export default async function Home(){const products=(await getProducts()).slice(0,3);return <main><section className="section" style={{minHeight:'82vh',display:'grid',placeItems:'center',background:'radial-gradient(circle at 15% 20%,rgba(197,160,89,.14),transparent 28%),linear-gradient(145deg,#2c3439,#111417)'}}><div className="wrap"><span className="kicker">{site.name}</span><h1 style={{fontSize:'clamp(42px,7vw,78px)',lineHeight:1.15,maxWidth:900}}>نحوّل الحرفة إلى <span className="gold">علامة</span>، والتصميم إلى قيمة.</h1><p className="muted" style={{fontSize:19,maxWidth:700}}>تنجيد • خياطة • تفصيل — منتجات مخصصة، خدمات تنفيذية، وتكوين مهني.</p><div style={{display:'flex',gap:12,flexWrap:'wrap',marginTop:28}}><Link className="btn primary" href="/products">استكشف المنتجات</Link><a className="btn secondary" href={whatsappLink('مرحباً ASLAN MODELLING، أريد طلب عرض.')} target="_blank" rel="noopener noreferrer">اطلب عرضًا</a></div></div></section><section className="section"><div className="wrap"><div className="head"><div><span className="kicker">COLLECTION</span><h2>مختارات ASLAN</h2></div><Link className="gold" href="/products">كل المنتجات ↗</Link></div><div className="grid three">{products.map(p=><article className="card product-card" key={p.id}><div className="product-image">{p.image_url?<img src={p.image_url} alt={p.name}/>:<span>ASLAN</span>}</div><h3>{p.name}</h3><p className="muted">{p.description}</p><div className="product-price">{p.price===null?'حسب الطلب':`${p.price} دج`}</div></article>)}</div></div></section><section className="section dark"><div className="wrap grid three"><div className="card"><span className="kicker">01</span><h2>تنجيد</h2><p className="muted">إعادة تنجيد وتجديد الأثاث والقطع التجارية.</p></div><div className="card"><span className="kicker">02</span><h2>خياطة</h2><p className="muted">تفصيل أقمشة ومنتجات حسب المقاس.</p></div><div className="card"><span className="kicker">03</span><h2>تفصيل</h2><p className="muted">تنفيذ قطع ومشاريع مخصصة من الفكرة إلى التشطيب.</p></div></div></section></main>}